module.exports = user =>({
    id: user.id,
    name: user.name,
    email: user.email,
    localization: user.localization,
    avatar: user.avatar,
    username: user.username,
    biography: user.biography,
})